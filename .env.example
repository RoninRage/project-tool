# Database
# For local development (SQLite file)
DATABASE_URL="file:./dev.db"

# For Docker (path inside container)
# DATABASE_URL="file:/app/data/dev.db"

# Seed on startup (Docker only)
SEED_DB=false
